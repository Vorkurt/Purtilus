import { SelectFieldPrimaryDirective } from './select-field-primary.directive';

describe('SelectFieldPrimaryDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectFieldPrimaryDirective();
    expect(directive).toBeTruthy();
  });
});
